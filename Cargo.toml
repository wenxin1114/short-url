[package]
name = "rs-web"
version = "0.1.0"
edition = "2024"

[dependencies]
actix-web = "3"
mysql = "*"
dotenv = "*" # 用于加载环境变量
rand = "*"
once_cell = "1.20.3"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
actix-files = "0.5.0"
